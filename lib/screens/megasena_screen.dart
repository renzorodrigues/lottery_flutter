import 'package:flutter/material.dart';

class MegaSenaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('MEGA-SENA'),
      ),
    );
  }
}
